<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Wikinimous</a>
    <button class="btn btn-sm btn-outline-primary" type="button"><%= link_to "New Article", new_path %></button>
  </div>
</nav>


<div class="container">
<% @articles.each do |article| %>
<h3><%= link_to article.title, article_path(article.id) %></h3>
<p><%= article.content %></p>
<% article.subtitle %>
<% article.paragraph %>
<% end %>
</div>
